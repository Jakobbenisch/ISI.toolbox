require(dygraphs)
require(ggplot2)
require(zoo)  #Constructor function for creating an extensible time-series object
require(xts)
require(signal)
require(lubridate)
require(partitions)
require(MASS)
require(lattice)
require(XML)
require(foreach)
require(plotly)
require(FME)

